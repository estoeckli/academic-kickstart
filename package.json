{
  "name": "Stoeckli2019",
  "private": true,
  "version": "1.0.0",
  "description": "Private Webpage",
  "scripts": {
    "postinstall": "",
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1 ./public",
    "pretest": "npm install",
    "preupdate-webdriver": "npm install",
    "update-webdriver": "webdriver-manager update"
  },
  "dependencies": {
    "express": "^4.16.3",
    "http-server": "^0.11.1"
  },
  "devDependencies": {
    "node-sass": "^4.9.3",
    "nodemon": "^1.18.4"
  }
}
